DROP TABLE IF EXISTS pet;
CREATE TABLE  pet (
                        id integer NOT NULL,
                        name varchar(50) NOT NULL,
                        tag varchar(50) NOT NULL,
                        PRIMARY KEY  (id)
);